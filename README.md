# virtual-memory-mapper
Simulador simples em Python para calcular automaticamente a tradução de endereços virtuais para endereços físicos (Paging).

## Como Usar:

1. Configure o sistema no código (tamanho de memória/memória virtual) ou use os Default
2. Faça o Maping na Page Table
3. Rode o programa e insira o endereço virtual
